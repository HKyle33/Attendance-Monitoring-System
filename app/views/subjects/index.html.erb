<p id="notice"><%= notice %></p>

<!-- ORIGINAL SCAFFOLD
<h1>Subjects</h1>

<table>
  <thead>
    <tr>
      <th>Subject name</th>
      <th>Subject code</th>
      <th>Units</th>
      <th>Day</th>
      <th>Time start</th>
      <th>Time end</th>
      <th>Class key</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subjects.each do |subject| %>
      <tr>
        <td><%= subject.subject_name %></td>
        <td><%= subject.subject_code %></td>
        <td><%= subject.units %></td>
        <td><%= subject.day %></td>
        <td><%= subject.time_start %></td>
        <td><%= subject.time_end %></td>
        <td><%= subject.class_key %></td>
        <td><%= link_to 'Show', subject %></td>
        <td><%= link_to 'Edit', edit_subject_path(subject) %></td>
        <td><%= link_to 'Destroy', subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject', new_subject_path %>
-->

<% if teacher_signed_in? %>
<div class="card-deck mb-3 mx-auto">

<% @subjects.each do |subject| %>
  <div class="card bg-light mb-3 " style="max-width: 30rem; min-width: 20rem">
    <div class="card-header bg-primary text-white h5"><br /><br />
      <%= subject.subject_code %>: <%= subject.subject_name %>
    </div>
    <div class="card-body">
      <div>Section: <%= subject.section %></div>
      <div>Units: <%= subject.units %></div>
      <div><%= subject.day %> | Insert Time Here </div>
      <!-- <div>Teacher: <%= subject.teacher.first_name %> <%= subject.teacher.last_name %></div> -->
      <!-- <div><%= subject.time_start %></div> -->
      <!-- <div><%= subject.time_end %></div>   -->
      <!-- <div>Class Key: <%= subject.class_key %></div> -->
    </div>
    <div class="card-footer">
      <%= link_to 'Show', subject %> -
      <%= link_to 'Edit', edit_subject_path(subject) %> - 
      <%= link_to 'Remove', subject, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  </div>
      <% end %>
 
<!-- NEW CLASS -->
<div class ="card bg-primary mb-3 " style="max-width: 30rem; min-width: 20rem; min-height:10rem">
 <div class="card-body ">
   <!-- EMPTY BODY -->
 </div>
<div class="card-footer text-white h5">
  <%= link_to 'Add Class', new_subject_path, style: 'color:#FFFFFF;' %></div>
</div>
<% else %>
  <%= render 'index_student', subject: @subject %>
<% end %>

